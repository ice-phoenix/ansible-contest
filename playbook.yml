---
- hosts: all
  become: true
  vars:
    update_apt_cache: true
  vars_files:
    - env_vars/base.yml
    - roles/base/defaults/main.yml
    
  roles:
    - base
    - rabbitmq
    - nginx
    - app
